# .github/workflows/ci-cd.yml
name: CI/CD Demo

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Checkout Code
        uses: actions/checkout@v3

      # - name: 🧱 Setup Node.js
      #   uses: actions/setup-node@v3
      #   with:
      #     node-version: 18

      # - name: 📦 Install Dependencies
      #   run: npm install

      - name: 🧪 Run Basic Test (opsional)
        run: echo "No tests yet"
        
      - name: Deploy to EC2
        env:
          PRIVATE_KEY: ${{ secrets.VPS_KEY }}
          HOSTNAME: ${{ secrets.VPS_HOST }}
          USERNAME: ${{ secrets.VPS_USER }}
        run: |
          echo "$PRIVATE_KEY" > micro_aws.pem
          chmod 600 micro_aws.pem
          ssh -v -o StrictHostKeyChecking=no -i private_key ${USERNAME}@${HOSTNAME} '
            cd /home/ubuntu/project/mobile-porto &&
            git pull origin main
